#include <stdio.h>

int main()  
{  
    int l, b, A;  
    printf("Enter length and breadth of the rectangle: ");  
    scanf("%d %d", &l, &b);  
    A = l * b;  
    printf("Area of the rectangle = %d\n", A);  
    return 0;  
}  
